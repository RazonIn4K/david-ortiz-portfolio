# Placeholder favicon.ico for legacy browser requests
# This file intentionally contains plain text (not binary ICO data).
# Modern browsers will use the SVG/PNG icons declared in index.html and manifest.json.
# Keeping this file prevents 404s from user agents that still probe /favicon.ico by default.

Instructions to generate a proper .ico (optional):
1) Open /assets/icons/generate-icons.html in a browser.
2) Right-click and save the desired sizes (e.g., 16x16, 32x32, 48x48) as PNGs.
3) Use an ICO generator (e.g., https://realfavicongenerator.net or ImageMagick) to create favicon.ico:
   - ImageMagick example:
     magick convert icon-16.png icon-32.png icon-48.png favicon.ico
4) Replace this file with the generated binary favicon.ico.

Note:
- This repository already includes:
  - /assets/icons/icon.svg
  - /assets/icons/icon-192.png
  - /assets/icons/icon-512.png
- These are referenced by link tags and the PWA manifest, covering modern browsers and platforms.
