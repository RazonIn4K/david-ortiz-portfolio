{
  "name": "personal-website",
  "version": "1.0.0",
  "type": "module",
  "description": "A professional portfolio website showcasing cloud support engineering and database optimization expertise. Built with modern web technologies and optimized for performance, accessibility, and SEO.",
  "main": "src/js/script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "python3 dev-server.py 9000",
    "dev-basic": "python3 -m http.server 8000",
    "serve": "python3 -m http.server 8000",
    "build": "node scripts/build-minify.js",
    "deploy": "echo \"Deploy to GitHub Pages or Vercel\"",
    "performance-audit": "lighthouse http://localhost:9000 --output json --only-categories=performance",
    "security-scan": "npm audit && echo 'Consider running: npx snyk test'",
    "optimize": "npm run build && npm run performance-audit",
    "lint": "stylelint 'src/**/*.css'",
    "lint-fix": "stylelint 'src/**/*.css' --fix",
    "remove-console": "node scripts/remove-console-logs.js",
    "build:prod": "npm run remove-console && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "@vercel/speed-insights": "^1.2.0"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2",
    "stylelint": "^16.24.0",
    "stylelint-config-standard": "^39.0.0",
    "terser": "^5.44.0"
  }
}
